# Last.fm API key (get yours at https://www.last.fm/api)
# Used by frontend as VITE_LASTFM_API_KEY (embedded at build time)
# The same key is used by the worker as LASTFM_API_KEY (set in .dev.vars)
VITE_LASTFM_API_KEY=your_lastfm_api_key

# Last.fm OAuth shared secret (server-side only, NEVER expose to frontend)
# Only needed for Last.fm authentication features
# Set in .dev.vars for local worker development
LASTFM_SHARED_SECRET=your_shared_secret_here
