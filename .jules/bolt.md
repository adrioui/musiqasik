## 2024-05-23 - Debouncing Resize Events **Learning:** Resize events can trigger hundreds of state updates per second, causing layout thrashing, especially in heavy components like D3 graphs. Debouncing is critical. **Action:** Use the custom `debounce` utility in `src/lib/utils.ts` (which supports `.cancel()`) for all window resize listeners to ensure performance and prevent memory leaks.
